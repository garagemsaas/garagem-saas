export interface Estoque { id: string; veiculoId: string; avaliacaoId: string | null; responsavelId: string; entrada: string; origem: string; valorAquisicao: number; precoAnunciado: number; precoMinimo: number; status: string; ordemServicoId: string | null; observacoes: string | null; revisao: number; placa: string | null; marca: string; modelo: string; custosPreparacao: number; custoTotal: number; margemPrevista: number }
export interface Avaliacao { id: string; veiculoId: string; clienteId: string | null; avaliadorId: string; data: string; km: number; valorEstimado: number; valorOferecido: number; validade: string; observacoes: string | null; status: string; revisao: number; placa: string | null; marca: string; modelo: string }
export interface Lead { id: string; clienteId: string; veiculoId: string | null; vendedorId: string; origem: string; status: string; observacoes: string | null; revisao: number; clienteNome: string; telefone: string; email: string | null; veiculoDescricao: string }
export interface Proposta { id: string; estoqueId: string; clienteId: string; vendedorId: string; leadId: string | null; status: string; numeroVersao: number; revisao: number; versaoId: string; precoAnunciado: number; valorNegociado: number; desconto: number; entrada: number; avaliacaoTrocaId: string | null; valorTroca: number; validade: string; observacoes: string | null; clienteNome: string; veiculoDescricao: string }
export interface Venda { id: string; estoqueId: string; veiculoId: string; clienteId: string; vendedorId: string; estoqueTrocaId: string | null; precoAnunciado: number; valorVendido: number; desconto: number; entrada: number; valorTroca: number; custoAcumulado: number; margemBruta: number; criadoEm: string; clienteNome: string; veiculoDescricao: string }
export interface Reserva { id: string; estoqueId: string; clienteId: string; vendedorId: string; validade: string; status: string; revisao: number }
export const rotulo = (s: string) => ({ EM_AVALIACAO: 'Em avaliação', EM_PREPARACAO: 'Preparando', DISPONIVEL: 'Disponível', RESERVADO: 'Reservado', VENDIDO: 'Vendido', ABERTA: 'Aberta', ACEITA: 'Aceita', RECUSADA: 'Recusada', EXPIRADA: 'Expirada', CANCELADA: 'Cancelada', RASCUNHO: 'Rascunho', ENVIADA: 'Enviada', NOVO: 'Novo', CONTATO_REALIZADO: 'Contato realizado', INTERESSADO: 'Interessado', PROPOSTA: 'Proposta', NEGOCIACAO: 'Negociação', PERDIDO: 'Perdido', ATIVA: 'Ativa', CONCLUIDA: 'Concluída' }[s] ?? s.replaceAll('_', ' '));
